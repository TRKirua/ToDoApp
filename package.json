{
  "scripts": {
    "dev": "tailwindcss -i src/css/input.css -o build/assets/styles.css --watch & parcel src/*.html --dist-dir build --public-url ./",
    "build:css": "tailwindcss -i src/css/input.css -o build/assets/styles.css --minify",
    "build:js": "parcel build src/*.html --dist-dir build --public-url ./",
    "build": "rm -rf build && npm run build:css && npm run build:js",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.0",
    "postcss": "^8.4.0",
    "parcel": "^2.8.0",
    "gh-pages": "^4.0.0"
  }
}
